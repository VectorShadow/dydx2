package gamestate;

public class GameStateUpdate {
    //todo - this class is responsible for specifying a specific individual update to the gamestate. Each event's
    // execute() method must return a list of these. The ZoneProcessor will then apply all updates for each execution
    // phase, then send the list over the datalink for the frontend to apply to its own gamestate.
}
